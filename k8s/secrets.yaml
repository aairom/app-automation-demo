apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: member-management
type: Opaque
stringData:
  VAULT_TOKEN: "dev-token"
  FLASK_SECRET_KEY: "production-secret-key-change-me"

# Made with Bob
